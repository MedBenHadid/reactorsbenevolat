{% extends 'base.html.twig' %}
{% block title %}Association {{ don.title }}{% endblock %}
{% block info %}Association {{ don.title }} :{% endblock %}
{% block bread %}<a href="{{ path('don_index') }}">Don</a> / {{ don.title }} {% endblock %}
{% block body %}
    <div class="properties-hero-area d-flex flex-wrap align-items-center mb-80">
        <div class="properties-slide">
            <!-- Properties Slider -->
            <div id="property-thumb-silde" class="carousel slide wow fadeInUp" data-wow-delay="200ms" data-ride="carousel">
                <ol class="carousel-indicators">
                    <li data-target="#property-thumb-silde" data-slide-to="0" class="active" style="background-image: url(img/bg-img/64.jpg);"></li>
                    <li data-target="#property-thumb-silde" data-slide-to="1" style="background-image: url(img/bg-img/65.jpg);"></li>
                    <li data-target="#property-thumb-silde" data-slide-to="2" style="background-image: url(img/bg-img/66.jpg);"></li>
                    <li data-target="#property-thumb-silde" data-slide-to="3" style="background-image: url(img/bg-img/67.jpg);"></li>
                </ol>

                <div class="carousel-inner">
                    <div class="carousel-item active">

                        <img src="{{ asset('uploads/images/donImages/'~don.image)}}" class="d-block w-100" alt="...">
                    </div>
                    <div class="carousel-item">
                        <img src="{{ asset('uploads/images/donImages/'~don.image)}}" class="d-block w-100" alt="...">
                    </div>
                    <div class="carousel-item">
                        <img src="{{ asset('uploads/images/donImages/'~don.image)}}" class="d-block w-100" alt="...">
                    </div>
                    <div class="carousel-item">
                        <img src="{{ asset('uploads/images/donImages/'~don.image)}}" class="d-block w-100" alt="...">
                    </div>
                </div>
            </div>
        </div>

        <!-- Properties Content Area -->
        <div class="properties-content-area wow fadeInUp" data-wow-delay="200ms">
            <div class="properties-content-info">
                <h2>{{ don.title }}</h2>
                <div class="properties-location">
                    <p><i class="fa fa-map-marker" aria-hidden="true"></i> {{ don.phone }} {{ don.address }}, {{ don.phone }}, TN</p>
                    <p><i class="fa fa-map-marker" aria-hidden="true"></i> address:  {{ don.address }}</p>
                </div>
                <h4 class="properties-rate">{{ don.description }} <span>/ Membres</span></h4>
                <p>Buy .</p>

            </div>
        </div>
    </div>


    <p class="form-row form-row-wide">
        <label for="password">Emplacement Association:
            <div id="map" style="width: 1000px; height: 300px; background-color: red"></div>
            <input type="hidden" name="lat_don" id="lat_demande" value="{{don.latitude }}" />
            <input type="hidden" name="lng_demande" id="lng_doné" value="{{don.longitude }}" />
        </label>
    </p>


    <ul>
        <li>
            <a href="{{ path('don_index') }}">Back to the list</a>
        </li>

    </ul>
    <script>

        function initMap() {
            var myLatLng = {lat: {{don.latitude}}, lng: {{don.longitude}}};

            var map = new google.maps.Map(document.getElementById('map'), {
                zoom: 10,
                center: myLatLng
            });

            var marker = new google.maps.Marker({
                position: myLatLng,
                map: map,

            });




        }
    </script>
    <script async defer
            src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD00UW-yFHi17pDyHLe19_ImRpo0ja5Q3k&callback=initMap">
    </script>

{% endblock %}
